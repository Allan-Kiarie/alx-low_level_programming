#include "main.h"
/**
 * _strstr - scans a string for another string
 * @haystack: string to scan
 * @needle: string to match
 * Return: pointer to first occurrence of substring,
 * null if no substring
 */
char *_strstr(char *haystack, char *needle)
{
}
